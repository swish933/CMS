<h2>All Logos</h2>

<div class="container mt-3 w-50">
    <table class='table table-hover'>
        <thead class="thead-dark">
            <tr>
                <th>Name</th>
                <th>Active</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            <% logos.forEach(logo => { %>
            <tr>
                <td><%=logo.name%></td>
                <td><%=logo.active%></td>
                <td class="d-flex">
                    <button class="btn btn-sm btn-warning mr-2">
                        <a href="/logo/<%=logo.id%>">Edit</a>
                    </button>
                    <form action='/logo/<%=logo.id%>?_method=DELETE' method="POST">
                        <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                    </form>

                </td>
            </tr>
            <% }) %>

        </tbody>
    </table>
</div>